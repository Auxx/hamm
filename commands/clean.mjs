import * as fs from 'fs/promises';
import * as path from 'path';

export async function cmdClean(env) {
  // const targetDir = env === undefined = 'build' ? ''

    console.log('Clean:', env);
}
